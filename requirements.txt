prompt_toolkit>=3.0.43
requests>=2.31
pmtiles>=3.4
pyserial>=3.5

# numpy and Pillow are the two compiled dependencies, and both sit on the frame
# path. The floors are per-Python because a floor low enough to predate the
# running interpreter lets pip fall back to building from source, and a
# source-built numpy measured about a quarter slower on the cell renderer than
# the released wheel. Each of these is the first release shipping a wheel for
# that CPython, so raise them when adding support for a newer one.
numpy>=1.26; python_version < "3.13"
numpy>=2.1; python_version == "3.13"
numpy>=2.3; python_version >= "3.14"

Pillow>=10.0.0; python_version < "3.13"
Pillow>=10.4.0; python_version == "3.13"
Pillow>=11.3.0; python_version >= "3.14"
